This directory contains a port of the QMI control interface to Genode. The app
directly communicates via a Terminal session to Genode 'usb_net' driver.

Configuration
~~~~~~~~~~~~~

The 'qmicli' component is configured in the same way as the 'mbimcli' component.
Please refer to _network/mbimcli/README_.
